# Core PDF processing
pymupdf>=1.23.0
pdfplumber>=0.10.0

# Data & output
openpyxl>=3.1.0
pandas>=2.0.0

# Web server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# LLM APIs
google-genai>=0.7.0
openai>=1.30.0
instructor>=1.3.0

# RAG (optional — pipeline works without these)
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0

# Schema / validation
pydantic>=2.7.0

# OCR (optional — scanned PDFs only)
# paddleocr>=2.7.0
# paddlepaddle>=2.6.0

# PDF generation (for mock data only)
reportlab>=4.0.0

# ── v4 additions (Neon DB pipeline) ──────────────────────────────────────────
# PostgreSQL driver (required for Phase 0–4 DB-backed pipeline)
psycopg2-binary>=2.9.9

# Fuzzy matching (Phase 3 Tier 2)
rapidfuzz>=3.6.0

# Environment variable loading
python-dotenv>=1.0.0
